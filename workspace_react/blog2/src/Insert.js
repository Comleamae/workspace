function Insert(props){
  return(
    <div className="insertBox">
      {props.titles.map(()=>{
        return(
          <button type="button" onClick={(e)=>{
            
          }}></button>
        )
      })

      }
    </div>
  );
}

export default Insert;